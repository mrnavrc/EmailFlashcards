@model EmailFlashcards.Models.Category

<div class="row justify-content-center mt-5">
    <div class="col-auto">
        <div class="card text-white bg-danger mb-3" style="max-width: 27rem;">
            <div class="card-header">
                <h4>Are you sure you want to delete this category?</h4>
            </div>
            <div class="card-body">
                <h6 class="card-title">
                    Name of your category: @Model.FlashcardCategoryName
                </h6>
                <form asp-action="Delete">
                    <div class="form-group mt-4">
                        <input type="submit" value="Delete" class="btn btn-dark text-white" />
                        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
